guidance:
  type: flow
  model_name: "raft_small"
  guidance_scale: 35.0
  guidance_steps_ratio: 0.4
  mode: "smoothness"
  flow_scale: 1.0

pipeline:
  model_id: "Wan-AI/Wan2.1-I2V-14B-480P-Diffusers"
  num_frames: 33
  num_inference_steps: 30
  cfg_scale: 5.0
  frame_subsample_rate: 4
  grad_clip: 1.0

output:
  fps: 16
  format: "mp4"
